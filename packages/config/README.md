# @bluemoon/config

Shared runtime application configuration and environment variable
parsing, consumed by `apps/web` and `apps/server`. **Placeholder — no
implementation yet.** See
[environment strategy](../../docs/engineering/environment-strategy.md).
