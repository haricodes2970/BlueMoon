# @bluemoon/eslint-config

Shared strict ESLint config for the workspace.

- `index.js` — base config (TypeScript strict + Prettier compatibility)
- `next.js` — extends base, adds Next.js rules for `apps/web`

See [coding standards](../../docs/engineering/coding-standards.md).
