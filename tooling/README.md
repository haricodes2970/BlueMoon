# tooling/

Shared development tooling consumed by every app and package in the
workspace, published as internal workspace packages:

- [`typescript-config`](./typescript-config) — strict base tsconfigs
- [`eslint-config`](./eslint-config) — strict ESLint flat config
- [`prettier-config`](./prettier-config) — shared formatting rules

Not application code — no feature logic lives here.
